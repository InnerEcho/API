# 로컬에 설치된 의존성 폴더는 이미지에 포함하지 않습니다.
# 컨테이너 내부에서 새로 설치하므로, 빌드 속도를 크게 향상시킵니다.
node_modules

# TypeScript 컴파일 결과물 폴더입니다.
# 컨테이너 내부 빌드 과정에서 생성되므로 로컬 파일은 필요 없습니다.
dist

# 환경 변수 파일은 민감한 정보를 담고 있으므로 이미지에 절대 포함해서는 안 됩니다.
.env*

# Git 버전 관리 폴더 및 관련 파일은 애플리케이션 실행에 필요 없습니다.
.git
.gitignore

# 로그 파일들은 실행 시 생성되므로 이미지에 포함할 필요가 없습니다.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dockerfile 자신과 .dockerignore 파일은 이미지 내부에 있을 필요가 없습니다.
Dockerfile
.dockerignore

# 코드 스타일, 빌드 설정 등 개발 환경에만 필요한 파일들입니다.
.prettierrc
tsconfig.json
README.md